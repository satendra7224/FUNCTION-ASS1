// IIFE to calculate and display the square of a number
(function(num) {
    const square = num * num;
    console.log(`The square of ${num} is ${square}.`);
})(5); // You can change 5 to any number you want to calculate the square for
