
const square = (num) => num * num;

// Example: Calculate the square of a given number
const number = 5;
const result = square(number);

// Display the result
console.log(`The square of ${number} is ${result}.`); // "The square of 5 is 25."
